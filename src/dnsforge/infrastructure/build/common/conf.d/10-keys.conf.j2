key "{{ TSIG_KEY_NAME }}" {

        algorithm hmac-sha256;

        secret "{{ TSIG_SECRET }}";
};


key "{{ RNDC_KEY_NAME }}" {

        algorithm hmac-sha256;

        secret "{{ RNDC_SECRET }}";
};
