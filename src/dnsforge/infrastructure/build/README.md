# Build Infrastructure Context

This directory contains build-time and generated infrastructure artifacts owned by the DNSForge infrastructure context.

```text
catalog/     DNS zone catalog
runtime/     runtime/generated build metadata
templates/   build-time templates when present
assets/      static build assets
```

The historical `src/build` directory is no longer used.
