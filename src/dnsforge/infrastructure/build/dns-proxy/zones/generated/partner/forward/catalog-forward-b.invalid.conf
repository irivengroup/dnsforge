zone "catalog-forward-b.invalid" {

        type forward;

        forward {{ DNS_FORWARD_POLICY }};

        forwarders {
                {{ AUTH_CLUSTER_B_BIND_LIST }}
        };

        allow-query {
                partner_clients;
        };
};
