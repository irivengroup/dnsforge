zone "split-example.invalid" {

        type master;

        file "/var/named/master/internal/split-example.invalid.zone";

        allow-query {
                recursive_clients;
        };

        dnssec-policy "{{ DNSSEC_POLICY_NAME }}";

        inline-signing yes;

        allow-transfer {
                none;
        };

        allow-update {
                none;
        };

        notify no;
};
