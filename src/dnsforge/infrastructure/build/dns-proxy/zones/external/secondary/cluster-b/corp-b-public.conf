zone "corp-b-public.invalid" {
        type secondary;
        file "/var/named/secondary/external/corp-b-public.invalid.zone";
        primaries {
                {{ AUTH_CLUSTER_B_PRIMARIES_BIND }}
        };
        allow-notify {
                {{ AUTH_CLUSTER_B_ALLOW_NOTIFY_BIND }}
        };
        allow-query {
                {{ ZONE_ACL_PUBLIC }}
        };

        allow-transfer {
                none;
        };
};
