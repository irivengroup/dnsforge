zone "example-edge-public.invalid" {

        type master;

        file "/var/named/master/external/example-edge-public.invalid.zone";

        allow-query {
                {{ ZONE_ACL_PUBLIC }}
        };

        allow-transfer {
                none;
        };

        allow-update {
                none;
        };

        notify no;
};
