zone "example-edge-internal.invalid" {

        type master;

        file "/var/named/master/internal/example-edge-internal.invalid.zone";

        allow-query {
                {{ ZONE_ACL_INTERNAL }}
        };

        allow-transfer {
                none;
        };

        allow-update {
                none;
        };

        notify no;
};
