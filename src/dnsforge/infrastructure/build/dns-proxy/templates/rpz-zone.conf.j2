zone "{{ RPZ_ZONE_NAME }}" {

        type master;

        file "/var/named/rpz/{{ RPZ_ZONE_NAME }}.zone";

        allow-query {
                none;
        };

        allow-transfer {
                none;
        };

        allow-update {
                none;
        };
};
