// DNSForge managed views.
view "internal" {
    match-clients { recursive_clients; localhost; };
    recursion yes;
    include "{{ INTERNAL_ZONE_INDEX }}";
};

view "external" {
    match-clients { any; };
    recursion no;
    include "{{ EXTERNAL_ZONE_INDEX }}";
};
